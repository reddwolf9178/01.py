6.1 无符号数和有符号数 · 笔记汇总
对应哈工大计组网课第6章第1节，408和专转本的核心基础。

一、无符号数
定义：没有符号位，所有二进制位都用来表示数值大小。

例子：8位无符号数范围是 0 ~ 255（即 0 ~ 2⁸-1）。

特点：

没有正负之分

常用于表示地址、计数器等非负场景

二、有符号数
有符号数需要一个符号位来表示正负。有三种编码方式：原码、反码、补码。

1. 原码
规则：最高位是符号位（0正1负），其余位是数值的绝对值。

例子（8位）：

+5 → 0000 0101

-5 → 1000 0101

特点：

直观，但0有两种表示：0000 0000（+0）和 1000 0000（-0）

加减法需要单独处理符号，电路复杂

2. 反码
规则：正数的反码 = 原码；负数的反码 = 符号位不变，数值位按位取反。

例子（8位）：

+5 → 0000 0101

-5 → 1111 1010

特点：

仍然有两个0：0000 0000（+0）和 1111 1111（-0）

主要用于求补码的中间步骤

3. 补码（重点）
规则：

正数的补码 = 原码

负数的补码 = 反码 + 1（即：符号位不变，数值位取反后加1）

例子（8位）：

+5 → 0000 0101

-5 → 1111 1011

特点：

0只有一种表示：0000 0000

加减法统一用加法实现，电路简单

计算机内部所有有符号数都用补码存储

补码的快速求法（从右往左，遇到第一个1及其右边的0不变，左边的位全部取反）：

-5 原码 1000 0101 → 从右往左第一个1是最后一位，保留它和右边的0（没有0），左边全部取反 → 1111 1011

三、三种编码对比（8位为例）
数值	原码	反码	补码
+5	0000 0101	0000 0101	0000 0101
-5	1000 0101	1111 1010	1111 1011
+0	0000 0000	0000 0000	0000 0000
-0	1000 0000	1111 1111	无（0唯一）
范围	-127~+127	-127~+127	-128~+127
注意：补码比原码多表示一个数 -128（1000 0000）。

四、补码的运算
1. 补码加法
直接相加，符号位参与运算，丢掉最高位的进位。

2. 补码减法
[A - B]补 = [A]补 + [-B]补，其中 [-B]补 是 [B]补 连同符号位一起取反加1。

3. 溢出判断
正溢出：两个正数相加，结果为负

负溢出：两个负数相加，结果为正

或者看最高位进位和次高位进位是否相同，不同则溢出

五、关键记忆点
补码是计算机唯一使用的有符号数表示法

正数的原码=反码=补码

负数的补码 = 反码 + 1

0在补码中只有一种表示

补码范围比原码多一个负数（-128）

补码减法转加法：A-B = A + (-B)补

